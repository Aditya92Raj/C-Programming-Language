//sizeof is a unary operator in C that is used to find the size of a data type, variable, array, structure, etc. in bytes.
#include <stdio.h>
int main()
{
    int a;
    printf("%d",sizeof(a));
    return 0;
}

//The Bitwise XOR (Exclusive OR) operator (^) compares the corresponding bits of two integer operands.
#include <stdio.h>
int main()
{
    int a=5,b=3;
    printf("%d",a^b);
    return 0;
}
//The result is 1 when the two bits are different and 0 when they are the same.
